function MergeSort(dataSet){
    if (dataSet.length === 1){
        return dataSet;
    }

    const midPoint = Math.floor(dataSet.length/2);
    const leftSubArray = dataSet.slice(0, dataSet.length/2);
    const rightSubArray = dataSet.slice(dataSet.length/2, dataSet.length);

}

function Merge(leftSubArray, rightSubArray) {
    
    
}


